<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.casadocodigo.boaviagem"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdx android:minSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar">
        <activity android:label="@string/app_name" android:name=".SplashScreen" android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".BoaViagemActivity" android:label="@string/app_name"/>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".ViagemActivity" />
        <activity android:name=".ViagemListActivity" />
        <activity android:name=".GastoActivity" />
        <activity android:name=".GastoListActivity" />
    </application>
</manifest>

